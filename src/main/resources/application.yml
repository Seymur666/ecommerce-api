spring:
  application:
    name: ecommerce-api
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_api
    username: postgres
    password: Seymur12345!
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

#  main:
#    allow-bean-definition-overriding: true

server:
  port: 6666

jwt:
  secret: MySuperSecretJwtKeyThatShouldBeLongEnough
  access:
    expiration: 3600000 # 1 hour
  refresh:
    expiration: 7200000 # 2 hours