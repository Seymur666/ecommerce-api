spring:
  application:
    name: ecommerce-api
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_api
    username: postgres
    password: Seymur12345!
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

#  main:
#    allow-bean-definition-overriding: true

server:
  port: 6666

jwt:
  secret: dGVzdC1zZWNyZXQtdG8tbWVldC1taW5pbXVtLWJpdC1yZXF1aXJlbWVudA==
  access:
    expiration: 900000   # 15 минут
  refresh:
    expiration: 604800000 # 7 дней

logging:
  level:
    org.springframework.security: DEBUG